<!-- Menu -->
<nav id="menu">
  <h2>Estácio TCC</h2>
  <ul>
    <% unless current_user.present? %>
      <li>
      <div class="row">
        <div class="col-sm-6"><%= link_to('Inscreva-se!', new_user_path) %></div>
        <div class="col-sm-6" id="fazerlogin"><%= link_to('Faça login', new_user_session_path) %></div>
      </div>
    <% end %>

    <li>

    <div class="row">
      <div class="col-sm-6"><%= link_to('Eventos', root_path) %></div>
      <% if current_user.present? && current_user.role == 'admin' %>
        <div class="col-sm-6"><%= link_to('Novo evento', new_talk_path) %></div>
      <% end %>
    </div>

    <li>
    
    <div class="row">
      <div class="col-sm-6"><%= link_to('Loja', lanchonetes_path) %></div>
      <% if current_user.present? && current_user.role == 'admin' %>
        <div class="col-sm-6"><%= link_to('Novo cardápio', new_lanchonete_path) %></div>
      <% end %>
    </div>

    <li>

    <% if current_user.present? %>
      <div class="col-sm-6"><%= link_to("Sair do usuário #{current_user.usuario}", destroy_user_session_path) %></div>
    <% end %>
  </ul>
</nav>